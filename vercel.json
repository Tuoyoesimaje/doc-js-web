{
  "buildCommand": "npm run build",
  "outputDirectory": "app/dist",
  "rewrites": [
    {
      "source": "/app/:path*",
      "destination": "/app/:path*"
    },
    {
      "source": "/app",
      "destination": "/app/index.html"
    }
  ]
}
